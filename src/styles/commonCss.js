import { css } from '@emotion/react';

export const cssPmT = css`
  font-size: 1rem;
  font-weight: 800;
  color: var(--primary-40);
  text-align: justify;
`;

export const cssPageTitle = css`
  font-size: 3rem;
  font-weight: 800;
  color: var(--blue-dark);
  text-align: left;
`;

export const cssMainTitle = css`
  font-size: 2.5rem;
  font-weight: 800;
  color: var(--blue-dark);
  text-align: justify;
`;
export const cssMainTitleM = css`
  ${cssMainTitle}
  font-size: 2rem;
`;

export const css25W800BD = css`
  text-align: justify;
  font-size: 1.25rem;
  font-weight: 800;
  color: var(--blue-dark);
`;

export const css30W800BD = css`
  text-align: justify;
  font-size: 1.5rem;
  font-weight: 800;
  color: var(--blue-dark);
`;

export const css40W800BD = css`
  text-align: justify;
  font-size: 2rem;
  font-weight: 800;
  color: var(--blue-dark);
`;

export const cssDes = css`
  font-size: 1rem;
  font-weight: 500;
  color: var(--gery-default);
  text-align: justify;
`;

export const cssLink = css`
  font-size: 1rem;
  font-weight: 700;
  color: var(--primary-70);
  text-align: justify;
  cursor: pointer;
`;

export const linediv = css`
  width: 100%;
  height: 1px;
  background-color: var(--gery-light);
`;
