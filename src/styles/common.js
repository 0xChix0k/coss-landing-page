import styled from '@emotion/styled';

export const StPmT = styled.div`
  font-size: 20px;
  font-weight: 800;
  color: var(--primary-40);
  text-align: justify;
`;

export const StPageTitle = styled.div`
  font-size: 60px;
  font-weight: 800;
  color: var(--blue-dark);
  text-align: justify;
`;

export const StMainTitle = styled.div`
  font-size: 50px;
  font-weight: 800;
  color: var(--blue-dark);
  text-align: justify;
`;

export const StDes = styled.div`
  font-size: 20px;
  font-weight: 500;
  color: var(--gery-default);
  text-align: justify;
`;

export const StLinkStr = styled.div`
  font-size: 20px;
  font-weight: 700;
  color: var(--primary-70);
  text-align: justify;
  margin-top: 16px;
  cursor: pointer;
`;
